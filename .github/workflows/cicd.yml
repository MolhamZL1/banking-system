name: Test & deploy to ghaymah

on:
  push:
    branches: [main]

jobs:
  # test:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Use Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20

  #     - name: Install deps
  #       run: npm ci

  #     - name: Run tests
  #       run: npm test

  # build_and_push_image:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - name: 📦 Checkout Code
  #       uses: actions/checkout@v5.0.0

  #     - name: 🔐 Login to DockerHub
  #       uses: docker/login-action@v3.6.0
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: 🐳 Build and Push Docker Image
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         push: true
  #         tags: ${{ secrets.DOCKERHUB_USERNAME }}/firstcicd:latest

  deploy:
    runs-on: ubuntu-latest
    #  needs: build_and_push_image
    steps:
      - name: 📦 Checkout Code
        uses: actions/checkout@v5.0.0

      - name: 🌩️ Install Ghaymah CLI
        run: curl -sSl https://cli.ghaymah.systems/install.sh | bash

      - name: 🔐 Login to Ghaymah Cloud
        run: $HOME/ghaymah/bin/gy auth login --email "${{ secrets.GHAYMAH_EMAIL }}" --password "${{ secrets.GHAYMAH_PW }}"

      - name: 🚀 Deploy Application
        run: $HOME/ghaymah/bin/gy resource app update
